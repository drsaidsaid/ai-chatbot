import "./styles.css";

export const metadata = {
  title: "AI Lead Employee",
  description: "WhatsApp lead handling operations",
};

export default function RootLayout({ children }) {
  return <html lang="en"><body>{children}</body></html>;
}
