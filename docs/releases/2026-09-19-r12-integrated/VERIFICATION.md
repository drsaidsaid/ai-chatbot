# R12 integrated acceptance — 19 September 2026

Candidate `a4d881a9de5ae5ba6fd8b588b4619df1d353a54c` merged without conflicts on accepted R20 baseline `e531463c5c784d36a6a0f887c2a511c614b878fd`. Integrated local acceptance passed; evidence included in normal integration commit. No deployment or live-provider verification is claimed.

Independent combined review cleared locale, Knowledge workspace, Account, routes and schema: commercial authority remains intact alongside review resolution, handoff feedback and explicit admin approval. Earlier source findings were corrected with regressions before browser acceptance.

Fresh owned `ale_r12_integrated_20260919_spec` loaded the exact R20 baseline schema; both R12 migrations applied normally. Synthetic pre-upgrade Account survived, commercial tables remained, and review suggestions gained the exact-one-source check and foreign keys. Logs `/tmp/r12-integrated-schema.log` and `/tmp/r12-integrated-migrate.log`.

Root actual Codex in-app desktop/390px acceptance proved canonical legacy redirects; public reply remains visibly pending after reload; switching conversations clears the previous Review identity; private notes stay out of separately authored knowledge; explicit draft then administrator approval; normal LeadHandoff feedback without fabricated Review; visible mobile admin queue and review without Offer-rule mutation. Readable qualification evidence replaced raw JSON. Final array/locale correction has a three-reason regression and limits rendered reasons to two.

The final synthetic database collector proves unchanged Offer version/digest, private Message preserved, approved KnowledgeItem containing only the separate public answer, and admin-reviewed handoff feedback. A deterministic KnowledgeAnswerService lookup returned newly approved item2 with its source and no private text or paid provider call. Durable candidate evidence is under `docs/releases/2026-09-19-r12/browser-acceptance/`.

Both root-created browser tabs and viewport overrides were cleaned. Owned Puma/Redis stopped. Candidate disclosed earlier hook bypasses; final candidate and this integration require normal hooks, with no retroactive claim for earlier commits.

Combined checks passed:34 backend examples,1 explicit guarded PostgreSQL concurrency example,45 frontend tests across Review panel/legacy redirect/Conversation Cockpit/Offer settings. Logs `/tmp/r12-integrated-spec.log`, `/tmp/r12-integrated-concurrency.log`, `/tmp/r12-integrated-vue.log`. Concurrency fixture cleanup was explicitly authorized only for the newly created exact root-owned database, after upgrade preservation had been verified.

Final integrated production build passed;232 manifest entries, no missing assets. Log `/tmp/r12-integrated-build.log`. All34 changed source/test hashes matched before commit.
