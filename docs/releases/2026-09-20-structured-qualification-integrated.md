# Structured qualification pilot repair

The live owner test supplied a business type, revenue and goal in Swahili, but the old conversation path repeated the English business-status question. This repair uses one existing metered managed-provider attempt to propose configured-field observations alongside a grounded answer. Local code validates types, quote context and current Offer authority, retains human evidence, evaluates the existing deterministic rules and chooses the next unanswered configured question. The provider receives the owner's actual prompts for language rendering. Ordinary managed accounts retain Launch Gate and customer allowance checks; the exact owner pilot retains its existing authorization and provider budget controls.

Reviewed candidates: 63d8e0c6, d4209f69, f9443888, fc28625f and final 1fa0f9df, based on 500ab767. Two-axis review required and verified fixes for Offer/Contact locking and request-time revision checks, numeric parsing, configured question context, conflicting observations, quoted clause context, generic routing, human handover and binding to localized emitted questions. The coordinator additionally reproduced an omitted-question bug (1 test failed): an empty prompt suffix falsely bound a later numeric reply. Both binding paths now require an actual nonblank string prompt, with a regression.

Canonical validation: 353 combined structured response, classifier, full R11 orchestration and relevant Offer request examples passed in 53.29 seconds. Coordinator's three modified files passed scoped RuboCop. The full changed qualification-mode request file also passed: 11 examples in 8.18 seconds, bringing canonical verification to 364 examples with zero failures. Tests use fake providers and isolated local data; these results do not establish live model quality.

No frontend, dependency or schema changes. Package a minimal backend derivative of deployed 500ab767 using verified parent image sha256:c916feec153b5ad07c1841a2a2e3acfaf3647f1b733a67577e69bbecf9375b94. Keep Pilot Authorization 2, provider revision 2, daily allowance 30, original expiry 2026-09-21T05:46:20Z, cumulative 30-attempt/USD2 approval and general Launch Gate pause intact. Pilot 1 remains revoked history. Take a fresh backup and verify full pilot-row fingerprints, schema equality and current usage before cutover. Coordinator alone deploys. Do not replay blocked messages; use a fresh owner-only message after release.

Next live acceptance: mixed Swahili answer and relevant nonrepeating question, English follow-up, editable-price handling, unknown-question review, strategy boundary, then stop/human takeover and visible team handover. At last live checkpoint only one provider attempt cost USD0.00008385; read current usage before further sends. Full R17/R18 and broader V1 acceptance remain open.
