diff --git a/spec/factories/lead_follow_ups.rb b/spec/factories/lead_follow_ups.rb
index b4f6031..550994f 100644
--- a/spec/factories/lead_follow_ups.rb
+++ b/spec/factories/lead_follow_ups.rb
@@ -19,6 +19,7 @@ FactoryBot.define do
                                            stage: stage, attempt_number: attempt_number)
     end
     after(:create) { |follow_up| follow_up.follow_up_attempt.update!(current_follow_up: follow_up) }
+
     status { :pending }
     stage { :incomplete_qualification }
     attempt_number { 1 }
diff --git a/spec/policies/offer_selection_policy_spec.rb b/spec/policies/conversation_policy_offer_selection_spec.rb
similarity index 67%
rename from spec/policies/offer_selection_policy_spec.rb
rename to spec/policies/conversation_policy_offer_selection_spec.rb
index 523d7a4..87061d2 100644
--- a/spec/policies/offer_selection_policy_spec.rb
+++ b/spec/policies/conversation_policy_offer_selection_spec.rb
@@ -2,12 +2,13 @@
 
 require 'rails_helper'
 
-RSpec.describe 'Conversation Offer selection policy' do
+# Conversation Offer selection policy.
+RSpec.describe ConversationPolicy do
   it 'does not grant Offer mutation to a bot that can read the Conversation' do
     account = create(:account)
     bot = create(:agent_bot, account: account)
     conversation = create(:conversation, account: account)
-    policy = ConversationPolicy.new({ user: bot, account: account, account_user: nil }, conversation)
+    policy = described_class.new({ user: bot, account: account, account_user: nil }, conversation)
 
     expect(policy.show?).to be(true)
     expect(policy.select_offer?).to be(false)
diff --git a/spec/requests/ai_lead_employee/offer_builtin_typed_rules_spec.rb b/spec/requests/ai_lead_employee/offer_builtin_typed_rules_spec.rb
index 9201218..a33d281 100644
--- a/spec/requests/ai_lead_employee/offer_builtin_typed_rules_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_builtin_typed_rules_spec.rb
@@ -3,13 +3,15 @@
 require 'rails_helper'
 
 RSpec.describe 'Typed rules for built-in Offer fields', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   [
     ['lead_volume', 'number', 'How many inquiries do you handle?', 'We receive 50 inquiries.', 'gte', 40, 50, 'positive'],
     ['decision_authority', 'boolean', 'Do you decide this purchase?', 'I am the owner.', 'eq', true, true, 'positive'],
     ['decision_authority', 'boolean', 'Do you decide this purchase?', 'I am not the decision maker.', 'eq', false, false, 'negative']
-  ].each do |key, type, prompt, statement, operator, comparison, typed_value, polarity|
+  ].each do |scenario|
+    key, type, prompt, statement, operator, comparison, typed_value, polarity = scenario
+
     it "applies a typed #{operator} rule to built-in #{key} #{polarity} evidence" do
       question = r09_question(key, answer_type: type, prompt: prompt)
       rule = { kind: 'score_rule', field: key, operator: operator, value: comparison, score_delta: 15, priority: 0, enabled: true }
diff --git a/spec/requests/ai_lead_employee/offer_capacity_continuations_spec.rb b/spec/requests/ai_lead_employee/offer_capacity_continuations_spec.rb
index 80291da..8c5f33b 100644
--- a/spec/requests/ai_lead_employee/offer_capacity_continuations_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_capacity_continuations_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Offer qualification from complete capacity statements', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   [
     ['USD', '1000.00', 'I need more leads now. I am the owner of the agency.', 'I can spend $2500, but my loan must be approved.'],
diff --git a/spec/requests/ai_lead_employee/offer_delivery_admission_races_spec.rb b/spec/requests/ai_lead_employee/offer_delivery_admission_races_spec.rb
index 4bcc37e..ba86a11 100644
--- a/spec/requests/ai_lead_employee/offer_delivery_admission_races_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_delivery_admission_races_spec.rb
@@ -5,7 +5,7 @@ require 'timeout'
 
 RSpec.describe 'Offer authority at the real provider admission transaction', type: :request do
   self.use_transactional_tests = false
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   let(:delivery_records) { {} }
 
@@ -14,14 +14,16 @@ RSpec.describe 'Offer authority at the real provider admission transaction', typ
   let(:provider_url) { %r{https://graph.facebook.com/v\d+\.\d+/[^/]+/messages} }
 
   prepend_before { clean_committed_fixtures }
+
   before do
     delivery_records[:offer_payload] = r09_create_offer
     delivery_records[:conversation] = r09_conversation(offer: delivery_records[:offer_payload])
     incoming, intent = r09_receive(delivery_records[:conversation], 'Hello')
     incoming.update!(provider_created_at: Time.current)
     delivery_records[:message] = intent.outbound_message
-    delivery_records[:request] = stub_request(:post, provider_url).to_return(status: 200,
-                                                                             body: '{"messages":[{"id":"wamid.R09.ADMITTED"}]}', headers: { 'Content-Type' => 'application/json' })
+    delivery_records[:request] = stub_request(:post, provider_url).to_return(
+      status: 200, body: '{"messages":[{"id":"wamid.R09.ADMITTED"}]}', headers: { 'Content-Type' => 'application/json' }
+    )
   end
 
   after do
@@ -60,7 +62,7 @@ RSpec.describe 'Offer authority at the real provider admission transaction', typ
   it 'holds Offer authority through dispatching commit while a later edit waits, then sends once' do
     database = ActiveRecord::Base.connection
     database.execute('SELECT pg_advisory_lock(95091)')
-    database.execute(<<~SQL)
+    database.execute(<<~SQL.squish)
       CREATE FUNCTION r09_pause_offer_admission() RETURNS trigger LANGUAGE plpgsql AS $$
       BEGIN IF NEW.state = 'dispatching' THEN PERFORM pg_advisory_xact_lock(95091); END IF; RETURN NEW; END $$;
       CREATE TRIGGER r09_pause_offer_admission BEFORE UPDATE ON whatsapp_outbound_deliveries
@@ -141,7 +143,8 @@ RSpec.describe 'Offer authority at the real provider admission transaction', typ
 
   def correct_evidence
     AiLeadEmployee::OfferHumanEvidenceWriter.new(conversation: Conversation.find(delivery_records[:conversation].id),
-                                                 offer: AiLeadEmployee::Offer.find(delivery_records[:offer_payload].fetch('id')), user: User.find(r09_admin.id),
+                                                 offer: AiLeadEmployee::Offer.find(delivery_records[:offer_payload].fetch('id')),
+                                                 user: User.find(r09_admin.id),
                                                  field_key: 'budget', value: 'TZS 600000').perform
   end
 
diff --git a/spec/requests/ai_lead_employee/offer_delivery_revision_spec.rb b/spec/requests/ai_lead_employee/offer_delivery_revision_spec.rb
index c25369a..7e32467 100644
--- a/spec/requests/ai_lead_employee/offer_delivery_revision_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_delivery_revision_spec.rb
@@ -4,7 +4,7 @@ require 'rails_helper'
 
 # Source-only preparation; no execution, real-provider call or green claim.
 RSpec.describe 'Offer revision at queued delivery boundaries', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   let(:provider_url) { %r{https://graph.facebook.com/v\d+\.\d+/[^/]+/messages} }
 
@@ -89,8 +89,12 @@ RSpec.describe 'Offer revision at queued delivery boundaries', type: :request do
   it 'cancels an old question after a human supplies its answer without changing the Offer configuration' do
     offer, conversation, message = queued_reply
     request = stub_delivery_for(message)
-    post "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence", headers: r09_headers,
-                                                                                       params: { offer_id: offer.fetch('id'), conversation_id: conversation.display_id, signal: 'budget', value: 'TZS 600000' }, as: :json
+    post(
+      "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence",
+      headers: r09_headers,
+      params: { offer_id: offer.fetch('id'), conversation_id: conversation.display_id, signal: 'budget', value: 'TZS 600000' },
+      as: :json
+    )
     expect(response).to have_http_status(:success)
     expect(AiLeadEmployee::Offer.find(offer.fetch('id')).configuration_version).to eq(offer.fetch('version'))
 
diff --git a/spec/requests/ai_lead_employee/offer_dependent_fact_tails_spec.rb b/spec/requests/ai_lead_employee/offer_dependent_fact_tails_spec.rb
index 37e911e..c2fb700 100644
--- a/spec/requests/ai_lead_employee/offer_dependent_fact_tails_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_dependent_fact_tails_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Dependent financial tails cannot create buying facts', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   [
     ['USD', '1000.00', 'I want to start now. I am the owner.', 'I can spend $2500, but I need it for groceries.'],
diff --git a/spec/requests/ai_lead_employee/offer_directory_filter_boundaries_spec.rb b/spec/requests/ai_lead_employee/offer_directory_filter_boundaries_spec.rb
index 7c1b947..d7563ba 100644
--- a/spec/requests/ai_lead_employee/offer_directory_filter_boundaries_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_directory_filter_boundaries_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Offer directory booking filters and effective sorting', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   def prepare_offer_pair_with_booking(status: :confirmed)
     first = r09_create_offer
@@ -71,7 +71,8 @@ RSpec.describe 'Offer directory booking filters and effective sorting', type: :r
       _first, _second, _booking, older = prepare_offer_pair_with_booking
       other_lead = create(:contact, account: account, name: 'Recent Lead', phone_number: '+255700111232')
       newer = r09_conversation(contact: other_lead)
-      r09_lead.conversations.update_all(last_activity_at: 2.hours.ago)
+      previous_activity = 2.hours.ago
+      r09_lead.conversations.each { |conversation| conversation.update!(last_activity_at: previous_activity) }
       newer.update!(last_activity_at: 1.hour.ago)
 
       get "/api/v1/accounts/#{account.id}/leads", headers: r09_headers, params: { sort: sort, direction: 'desc' }
diff --git a/spec/requests/ai_lead_employee/offer_evidence_edit_boundaries_spec.rb b/spec/requests/ai_lead_employee/offer_evidence_edit_boundaries_spec.rb
index b898809..0d758a2 100644
--- a/spec/requests/ai_lead_employee/offer_evidence_edit_boundaries_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_evidence_edit_boundaries_spec.rb
@@ -3,11 +3,15 @@
 require 'rails_helper'
 
 RSpec.describe 'Offer evidence edit boundaries', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   def edit_evidence(offer, conversation, **attributes)
-    post "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence", headers: r09_headers,
-                                                                                       params: { offer_id: offer.fetch('id'), conversation_id: conversation.display_id, signal: 'budget', value: 'TZS 250000' }.merge(attributes), as: :json
+    post(
+      "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence",
+      headers: r09_headers,
+      params: { offer_id: offer.fetch('id'), conversation_id: conversation.display_id, signal: 'budget', value: 'TZS 250000' }.merge(attributes),
+      as: :json
+    )
   end
 
   it 'rejects a different Offer from the current Conversation selection without writing any evidence' do
@@ -15,7 +19,7 @@ RSpec.describe 'Offer evidence edit boundaries', type: :request do
     second = r09_create_offer(r09_configuration(name: 'Second Offer'))
     conversation = r09_conversation(offer: first)
 
-    expect { edit_evidence(second, conversation) }.not_to(change { QualificationEvidence.count })
+    expect { edit_evidence(second, conversation) }.not_to(change(QualificationEvidence, :count))
     expect(response).to have_http_status(:unprocessable_entity)
   end
 
@@ -24,7 +28,7 @@ RSpec.describe 'Offer evidence edit boundaries', type: :request do
     r09_conversation(offer: offer)
     other = r09_conversation(offer: offer, contact: create(:contact, account: account))
 
-    expect { edit_evidence(offer, other) }.not_to(change { QualificationEvidence.count })
+    expect { edit_evidence(offer, other) }.not_to(change(QualificationEvidence, :count))
     expect(response).to have_http_status(:not_found)
   end
 
@@ -36,9 +40,13 @@ RSpec.describe 'Offer evidence edit boundaries', type: :request do
     hidden = r09_conversation(offer: offer)
 
     expect do
-      post "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence", headers: member.create_new_auth_token,
-                                                                                         params: { offer_id: offer.fetch('id'), conversation_id: hidden.display_id, signal: 'budget', value: 'TZS 250000' }, as: :json
-    end.not_to(change { QualificationEvidence.count })
+      post(
+        "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence",
+        headers: member.create_new_auth_token,
+        params: { offer_id: offer.fetch('id'), conversation_id: hidden.display_id, signal: 'budget', value: 'TZS 250000' },
+        as: :json
+      )
+    end.not_to(change(QualificationEvidence, :count))
     expect(response).to have_http_status(:not_found)
   end
 
diff --git a/spec/requests/ai_lead_employee/offer_evidence_lifecycle_spec.rb b/spec/requests/ai_lead_employee/offer_evidence_lifecycle_spec.rb
index 7e7f654..137b17a 100644
--- a/spec/requests/ai_lead_employee/offer_evidence_lifecycle_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_evidence_lifecycle_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Offer evidence lifecycle', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   it 'qualifies explicit purchase spending capacity and retains its basis and original message' do
     questions = [
@@ -78,8 +78,12 @@ RSpec.describe 'Offer evidence lifecycle', type: :request do
     r09_receive(conversation_b, 'My budget is TZS 1000000.')
     other_snapshot = r09_qualification(second).evidence_snapshot
 
-    post "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence", headers: r09_headers,
-                                                                                       params: { offer_id: first.fetch('id'), conversation_id: conversation_a.display_id, signal: 'budget', value: 'TZS 250000' }, as: :json
+    post(
+      "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence",
+      headers: r09_headers,
+      params: { offer_id: first.fetch('id'), conversation_id: conversation_a.display_id, signal: 'budget', value: 'TZS 250000' },
+      as: :json
+    )
 
     expect(response).to have_http_status(:success)
     expect(response.parsed_body).to include('offer_id' => first.fetch('id'))
@@ -107,9 +111,7 @@ RSpec.describe 'Offer evidence lifecycle', type: :request do
     r09_update_offer(first, budget_ranges: ranges)
     expect(response).to have_http_status(:success)
     updated = response.parsed_body
-    expect(r09_qualification(first).stale_at).to be_present
-    expect(r09_qualification(first).evidence_snapshot).to eq(original_evidence)
-    expect(r09_qualification(second).attributes).to eq(unchanged)
+    verify_configuration_invalidation(first, second, original_evidence, unchanged)
 
     r09_receive(conversation_a, 'Hello')
 
@@ -157,4 +159,10 @@ RSpec.describe 'Offer evidence lifecycle', type: :request do
     expect(replied.outbound_message.additional_attributes.dig('ai_lead_employee', 'qualification', 'next_question')).to be_nil
     expect(replied.outbound_message.content).not_to include('?')
   end
+
+  def verify_configuration_invalidation(first, second, original_evidence, unchanged)
+    expect(r09_qualification(first).stale_at).to be_present
+    expect(r09_qualification(first).evidence_snapshot).to eq(original_evidence)
+    expect(r09_qualification(second).attributes).to eq(unchanged)
+  end
 end
diff --git a/spec/requests/ai_lead_employee/offer_follow_up_lifecycle_races_spec.rb b/spec/requests/ai_lead_employee/offer_follow_up_lifecycle_races_spec.rb
index 2b588f2..c0c6b02 100644
--- a/spec/requests/ai_lead_employee/offer_follow_up_lifecycle_races_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_follow_up_lifecycle_races_spec.rb
@@ -5,7 +5,7 @@ require 'timeout'
 
 RSpec.describe 'Offer follow-up committed lifecycle interleavings', type: :request do
   self.use_transactional_tests = false
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   let(:delivery_records) { {} }
 
@@ -13,6 +13,7 @@ RSpec.describe 'Offer follow-up committed lifecycle interleavings', type: :reque
   let(:release) { Queue.new }
 
   prepend_before { clean_committed_fixtures }
+
   before do
     delivery_records[:offer] = r09_create_offer
     delivery_records[:conversation] = r09_conversation(offer: delivery_records[:offer])
@@ -40,10 +41,7 @@ RSpec.describe 'Offer follow-up committed lifecycle interleavings', type: :reque
       winner == 'replacement' ? interleave(-> { replace }, -> { accept }) : interleave(-> { accept }, -> { replace })
 
       if winner == 'replacement'
-        expect(delivery_records[:follow_up].reload.superseded_at).to be_present
-        expect(delivery_records[:delivery].reload).to be_canceled
-        expect(delivery_records[:attempt].reload).to be_unadmitted
-        expect(delivery_records[:attempt].current_follow_up.message_id).to be_nil
+        verify_replacement_winner
       else
         expect(delivery_records[:delivery].reload).to be_accepted
         expect(delivery_records[:attempt].reload).to be_accepted
@@ -56,9 +54,9 @@ RSpec.describe 'Offer follow-up committed lifecycle interleavings', type: :reque
   %w[cancellation acceptance].each do |winner|
     it "preserves the admission boundary when #{winner} wins against control cancellation" do
       prime_owner(admitted: winner == 'acceptance')
-      cancel = -> {
- Whatsapp::OutboundDelivery.cancel_automation!(conversation: Conversation.find(delivery_records[:conversation].id), reason: 'control_changed')
-}
+      cancel = lambda do
+        Whatsapp::OutboundDelivery.cancel_automation!(conversation: Conversation.find(delivery_records[:conversation].id), reason: 'control_changed')
+      end
       winner == 'cancellation' ? interleave(cancel, -> { accept }) : interleave(-> { accept }, cancel)
 
       expected = winner == 'cancellation' ? 'blocked' : 'accepted'
@@ -144,9 +142,8 @@ RSpec.describe 'Offer follow-up committed lifecycle interleavings', type: :reque
       end
       Timeout.timeout(15) { held.pop }
       action = {
-        'acceptance' => -> { accept }, 'preparation_failure' => -> {
- Whatsapp::OutboundDelivery.find(delivery_records[:delivery].id).fail_preparation!
-},
+        'acceptance' => -> { accept },
+        'preparation_failure' => -> { Whatsapp::OutboundDelivery.find(delivery_records[:delivery].id).fail_preparation! },
         'repair' => -> { Whatsapp::OutboundDelivery.find(delivery_records[:delivery].id).reconcile! },
         'recovery' => -> { Whatsapp::OutboundDelivery.find(delivery_records[:delivery].id).recover! }
       }.fetch(operation)
@@ -415,13 +412,13 @@ RSpec.describe 'Offer follow-up committed lifecycle interleavings', type: :reque
     contender.value
   end
 
-  def start_worker(&block)
+  def start_worker
     pid = Queue.new
     worker = Thread.new do
       ActiveRecord::Base.connection_pool.with_connection do |connection|
         connection.execute("SET lock_timeout = '12s'")
         pid << connection.raw_connection.backend_pid
-        block.call
+        yield
       ensure
         connection.execute('RESET lock_timeout')
       end
@@ -439,11 +436,20 @@ RSpec.describe 'Offer follow-up committed lifecycle interleavings', type: :reque
   end
 
   def clean_committed_fixtures
-    raise 'Dedicated Rails test database required' unless Rails.env.test? && ActiveRecord::Base.connection.current_database == 'ale_r09_offers_20260911_spec'
+    unless Rails.env.test? && ActiveRecord::Base.connection.current_database == 'ale_r09_offers_20260911_spec'
+      raise 'Dedicated Rails test database required'
+    end
 
     database = ActiveRecord::Base.connection
     tables = database.tables - %w[schema_migrations ar_internal_metadata installation_configs]
     database.execute("TRUNCATE #{tables.map { |table| database.quote_table_name(table) }.join(', ')} CASCADE")
     clear_enqueued_jobs
   end
+
+  def verify_replacement_winner
+    expect(delivery_records[:follow_up].reload.superseded_at).to be_present
+    expect(delivery_records[:delivery].reload).to be_canceled
+    expect(delivery_records[:attempt].reload).to be_unadmitted
+    expect(delivery_records[:attempt].current_follow_up.message_id).to be_nil
+  end
 end
diff --git a/spec/requests/ai_lead_employee/offer_follow_up_lineage_spec.rb b/spec/requests/ai_lead_employee/offer_follow_up_lineage_spec.rb
index 59d9a98..41cbb4b 100644
--- a/spec/requests/ai_lead_employee/offer_follow_up_lineage_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_follow_up_lineage_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Offer follow-up attempt history', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   def schedule(conversation)
     result = AiLeadEmployee::QualificationService.new(conversation: conversation).perform
@@ -25,10 +25,7 @@ RSpec.describe 'Offer follow-up attempt history', type: :request do
       expect(response).to have_http_status(:success)
       offer = response.parsed_body
       successor = schedule(conversation)
-      expect(successor.id).not_to eq(current.id)
-      expect(successor.replaces_follow_up_id).to eq(current.id)
-      expect(current.reload.replaced_by_follow_up_id).to eq(successor.id)
-      expect(current.superseded_at).to be_present
+      verify_replacement_link(current, successor)
       current = successor
     end
     expect(first.reload.attributes.slice(*original.keys)).to eq(original)
@@ -117,4 +114,11 @@ RSpec.describe 'Offer follow-up attempt history', type: :request do
     expect(other.follow_up_attempt.current_follow_up_id).to eq(other.id)
     expect(LeadFollowUpAttempt.where(account: account, contact: r09_lead).count).to eq(2)
   end
+
+  def verify_replacement_link(current, successor)
+    expect(successor.id).not_to eq(current.id)
+    expect(successor.replaces_follow_up_id).to eq(current.id)
+    expect(current.reload.replaced_by_follow_up_id).to eq(successor.id)
+    expect(current.superseded_at).to be_present
+  end
 end
diff --git a/spec/requests/ai_lead_employee/offer_follow_up_outcomes_spec.rb b/spec/requests/ai_lead_employee/offer_follow_up_outcomes_spec.rb
index da4f964..e1b3e9a 100644
--- a/spec/requests/ai_lead_employee/offer_follow_up_outcomes_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_follow_up_outcomes_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Offer follow-up canonical delivery outcomes', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   let(:delivery_records) { {} }
   let(:provider_url) { %r{https://graph.facebook.com/v\d+\.\d+/[^/]+/messages} }
diff --git a/spec/requests/ai_lead_employee/offer_follow_up_terminal_invalidation_spec.rb b/spec/requests/ai_lead_employee/offer_follow_up_terminal_invalidation_spec.rb
index 7262205..59f6f7d 100644
--- a/spec/requests/ai_lead_employee/offer_follow_up_terminal_invalidation_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_follow_up_terminal_invalidation_spec.rb
@@ -3,20 +3,12 @@
 require 'rails_helper'
 
 RSpec.describe 'Stronger invalidation of a context-canceled follow-up', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   let(:delivery_records) { {} }
 
   before do
-    delivery_records[:offer] = r09_create_offer
-    delivery_records[:conversation] = r09_conversation(offer: delivery_records[:offer])
-    delivery_records[:follow_up] = schedule.first
-    delivery_records[:follow_up].update!(scheduled_at: 1.minute.ago)
-    r09_update_offer(delivery_records[:offer], name: 'Changed before follow-up materialization')
-    expect(response).to have_http_status(:success)
-    AiLeadEmployee::FollowUpDeliveryService.new(follow_up: delivery_records[:follow_up]).perform
-    expect(delivery_records[:follow_up].reload).to have_attributes(status: 'cancelled', cancellation_reason: 'offer_configuration_changed')
-    expect(delivery_records[:follow_up].follow_up_attempt).to be_unadmitted
+    prepare_context_cancelled_follow_up
   end
 
   %w[control provider opt_out].each do |authority|
@@ -62,17 +54,43 @@ RSpec.describe 'Stronger invalidation of a context-canceled follow-up', type: :r
                                                                      params: { api_key: 'synthetic-r09-restored-provider-key' }, as: :json
       expect(response).to have_http_status(:success)
     when 'opt_out'
-      message = create(:message, account: account, inbox: delivery_records[:conversation].inbox, conversation: delivery_records[:conversation],
-                                 sender: r09_lead, message_type: :incoming, content: 'Please resume messaging me again',
-                                 source_id: 'wamid.R09.EXPLICIT.RECONSENT', provider_created_at: Time.current)
-      receipt = Whatsapp::WebhookReceipt.create!(raw_body: '{"synthetic":"r09-reconsent"}', body_digest: SecureRandom.hex(32),
-                                                 verified_routes: [r09_channel.id], expanded_at: Time.current)
-      Whatsapp::WebhookEvent.create!(receipt: receipt, account: account, inbox: delivery_records[:conversation].inbox, channel: r09_channel,
-                                     event_key: SecureRandom.hex(16), kind: 'messages', state: :processed,
-                                     provider_message_id: message.source_id, payload: { 'synthetic' => 'verified reconsent fixture' })
-      AiLeadEmployee::AutomatedContactConsent.grant!(contact: r09_lead, source_message: message,
-                                                     expected_event_id: AiLeadEmployee::AutomatedContactConsent.withdrawal_token(delivery_records[:stop]), actor: r09_admin)
-      expect(LeadFollowUpOptOut.exists?(account: account, contact: r09_lead)).to be(false)
+      restore_explicit_consent
     end
   end
+
+  def prepare_context_cancelled_follow_up
+    schedule_initial_follow_up
+    r09_update_offer(delivery_records[:offer], name: 'Changed before follow-up materialization')
+    expect(response).to have_http_status(:success)
+    AiLeadEmployee::FollowUpDeliveryService.new(follow_up: delivery_records[:follow_up]).perform
+    expect(delivery_records[:follow_up].reload).to have_attributes(status: 'cancelled', cancellation_reason: 'offer_configuration_changed')
+    expect(delivery_records[:follow_up].follow_up_attempt).to be_unadmitted
+  end
+
+  def restore_explicit_consent
+    message = create(:message, account: account, inbox: delivery_records[:conversation].inbox, conversation: delivery_records[:conversation],
+                               sender: r09_lead, message_type: :incoming, content: 'Please resume messaging me again',
+                               source_id: 'wamid.R09.EXPLICIT.RECONSENT', provider_created_at: Time.current)
+    record_verified_reconsent(message)
+    AiLeadEmployee::AutomatedContactConsent.grant!(
+      contact: r09_lead, source_message: message,
+      expected_event_id: AiLeadEmployee::AutomatedContactConsent.withdrawal_token(delivery_records[:stop]), actor: r09_admin
+    )
+    expect(LeadFollowUpOptOut.exists?(account: account, contact: r09_lead)).to be(false)
+  end
+
+  def schedule_initial_follow_up
+    delivery_records[:offer] = r09_create_offer
+    delivery_records[:conversation] = r09_conversation(offer: delivery_records[:offer])
+    delivery_records[:follow_up] = schedule.first
+    delivery_records[:follow_up].update!(scheduled_at: 1.minute.ago)
+  end
+
+  def record_verified_reconsent(message)
+    receipt = Whatsapp::WebhookReceipt.create!(raw_body: '{"synthetic":"r09-reconsent"}', body_digest: SecureRandom.hex(32),
+                                               verified_routes: [r09_channel.id], expanded_at: Time.current)
+    Whatsapp::WebhookEvent.create!(receipt: receipt, account: account, inbox: delivery_records[:conversation].inbox, channel: r09_channel,
+                                   event_key: SecureRandom.hex(16), kind: 'messages', state: :processed,
+                                   provider_message_id: message.source_id, payload: { 'synthetic' => 'verified reconsent fixture' })
+  end
 end
diff --git a/spec/requests/ai_lead_employee/offer_independent_builtin_tails_spec.rb b/spec/requests/ai_lead_employee/offer_independent_builtin_tails_spec.rb
index 0683415..7a4c73b 100644
--- a/spec/requests/ai_lead_employee/offer_independent_builtin_tails_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_independent_builtin_tails_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Independent built-in answers after a capacity statement', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   [
     ['USD', '1000.00', 'I can spend $2500, but I handle 20 leads.', 'lead_volume', { 'typed_value' => 20 }],
diff --git a/spec/requests/ai_lead_employee/offer_legacy_lead_edit_spec.rb b/spec/requests/ai_lead_employee/offer_legacy_lead_edit_spec.rb
index 30d6711..11e45e3 100644
--- a/spec/requests/ai_lead_employee/offer_legacy_lead_edit_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_legacy_lead_edit_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Legacy Lead edits when Offers are configured', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   it 'rejects legacy evidence atomically with an explicit-context error instead of reporting an ignored correction as saved' do
     offer = r09_create_offer(r09_configuration)
@@ -13,8 +13,12 @@ RSpec.describe 'Legacy Lead edits when Offers are configured', type: :request do
     original_snapshot = r09_qualification(offer).evidence_snapshot
 
     expect do
-      patch "/api/v1/accounts/#{account.id}/leads/#{r09_lead.id}", headers: r09_headers,
-                                                                   params: { lead: { name: 'Must not partially save', evidence: { budget: 'TZS 250000' } } }, as: :json
+      patch(
+        "/api/v1/accounts/#{account.id}/leads/#{r09_lead.id}",
+        headers: r09_headers,
+        params: { lead: { name: 'Must not partially save', evidence: { budget: 'TZS 250000' } } },
+        as: :json
+      )
     end.not_to(change { QualificationEvidence.where(account: account).count })
 
     expect(response).to have_http_status(:unprocessable_entity)
diff --git a/spec/requests/ai_lead_employee/offer_reply_context_spec.rb b/spec/requests/ai_lead_employee/offer_reply_context_spec.rb
index 0f8804c..26cc770 100644
--- a/spec/requests/ai_lead_employee/offer_reply_context_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_reply_context_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Frozen Offer context for normal replies and handoffs', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   let(:provider_url) { %r{https://graph.facebook.com/v\d+\.\d+/[^/]+/messages} }
 
@@ -99,7 +99,7 @@ RSpec.describe 'Frozen Offer context for normal replies and handoffs', type: :re
     account.update!(settings: account.settings.deep_merge('ai_lead_employee' => {
                                                             'alert_routes' => { AiLeadEmployee::HighlyQualifiedHandoffService::ALERT_TYPE => [{
                                                               'type' => 'whatsapp', 'recipient' => '255700000094'
-}] }
+                                                            }] }
                                                           }))
     incoming = create(:message, account: account, inbox: conversation.inbox, conversation: conversation,
                                 sender: conversation.contact, message_type: :incoming,
@@ -115,8 +115,12 @@ RSpec.describe 'Frozen Offer context for normal replies and handoffs', type: :re
 
   it 'rejects the original handoff decision after a same-configuration human correction' do
     offer, conversation, result = handoff_scenario
-    post "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence", headers: r09_headers,
-                                                                                       params: { offer_id: offer.fetch('id'), conversation_id: conversation.display_id, signal: 'budget', value: 'USD 100' }, as: :json
+    post(
+      "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence",
+      headers: r09_headers,
+      params: { offer_id: offer.fetch('id'), conversation_id: conversation.display_id, signal: 'budget', value: 'USD 100' },
+      as: :json
+    )
     expect(response).to have_http_status(:success)
     expect(make_handoff(conversation, result).handoff).to be_nil
     expect(conversation.reload).to have_attributes(control_state: 'ai_active', assignee_id: nil)
@@ -136,8 +140,12 @@ RSpec.describe 'Frozen Offer context for normal replies and handoffs', type: :re
       if change == 'configuration'
         r09_update_offer(offer, name: 'Edited after handoff')
       else
-        post "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence", headers: r09_headers,
-                                                                                           params: { offer_id: offer.fetch('id'), conversation_id: conversation.display_id, signal: 'budget', value: 'USD 100' }, as: :json
+        post(
+          "/api/v1/accounts/#{account.id}/lead_qualifications/#{r09_lead.id}/evidence",
+          headers: r09_headers,
+          params: { offer_id: offer.fetch('id'), conversation_id: conversation.display_id, signal: 'budget', value: 'USD 100' },
+          as: :json
+        )
       end
       expect(response).to have_http_status(:success)
       2.times { SendReplyJob.perform_now(message.id) }
diff --git a/spec/requests/ai_lead_employee/offer_selection_authority_spec.rb b/spec/requests/ai_lead_employee/offer_selection_authority_spec.rb
index c9eaac6..5e42eab 100644
--- a/spec/requests/ai_lead_employee/offer_selection_authority_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_selection_authority_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Offer selection authority', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   let(:member) { create(:user, account: account, role: :agent) }
   let(:initial_offer) { r09_create_offer(r09_configuration) }
diff --git a/spec/requests/ai_lead_employee/offer_shared_readers_spec.rb b/spec/requests/ai_lead_employee/offer_shared_readers_spec.rb
index 2d30fa5..05137f0 100644
--- a/spec/requests/ai_lead_employee/offer_shared_readers_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_shared_readers_spec.rb
@@ -4,7 +4,7 @@ require 'rails_helper'
 
 # Acceptance specification for the approved additive reader contract.
 RSpec.describe 'Offer context in shared qualification readers', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   def prepare_two_qualified_offers
     first = r09_create_offer(r09_configuration)
diff --git a/spec/requests/ai_lead_employee/offer_typed_answer_boundaries_spec.rb b/spec/requests/ai_lead_employee/offer_typed_answer_boundaries_spec.rb
index fa01aa8..91341a2 100644
--- a/spec/requests/ai_lead_employee/offer_typed_answer_boundaries_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_typed_answer_boundaries_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Typed Offer answer boundaries', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   [
     ['TZS 1200000 if my contract is signed', 'money', {}],
diff --git a/spec/requests/ai_lead_employee/offer_typed_field_rules_spec.rb b/spec/requests/ai_lead_employee/offer_typed_field_rules_spec.rb
index def0b90..004a99e 100644
--- a/spec/requests/ai_lead_employee/offer_typed_field_rules_spec.rb
+++ b/spec/requests/ai_lead_employee/offer_typed_field_rules_spec.rb
@@ -3,7 +3,7 @@
 require 'rails_helper'
 
 RSpec.describe 'Typed Offer answers and qualification rules', type: :request do
-  include_context 'Offer qualification requests'
+  include_context 'with Offer qualification requests'
 
   it 'binds a revenue amount to the actual outgoing question without converting it into budget or inquiry volume' do
     question = r09_question('monthly_revenue', answer_type: 'money', prompt: 'What is your monthly business revenue?', period: 'month')
@@ -29,7 +29,9 @@ RSpec.describe 'Typed Offer answers and qualification rules', type: :request do
     ['uses_crm', 'boolean', 'Do you currently use a CRM?', 'Hapana', false, 'negative', {}],
     ['customer_type', 'choice', 'Are your customers B2B or B2C?', 'B2B', 'B2B', 'positive', { options: %w[B2B B2C] }],
     ['support_topic', 'text', 'Which part of your operations needs support?', 'Remote onboarding', 'Remote onboarding', 'positive', {}]
-  ].each do |key, type, prompt, answer, typed_value, polarity, options|
+  ].each do |scenario|
+    key, type, prompt, answer, typed_value, polarity, options = scenario
+
     it "records a verified #{type} answer as a typed fact and does not ask it again" do
       question = r09_question(key, answer_type: type, prompt: prompt, **options)
       offer = r09_create_offer(r09_configuration(questions: [question]))
@@ -155,6 +157,7 @@ RSpec.describe 'Typed Offer answers and qualification rules', type: :request do
     expect(account.qualification_offers).to be_empty
   end
 
+  rule_defaults = { kind: 'score_rule', priority: 0, score_delta: 30, enabled: true }.freeze
   [
     { field: 'missing_field', operator: 'gte', value: 10 },
     { field: 'team_size', operator: 'execute', value: 'arbitrary expression' },
@@ -164,7 +167,7 @@ RSpec.describe 'Typed Offer answers and qualification rules', type: :request do
   ].each do |invalid_attributes|
     it "rejects an invalid rule #{invalid_attributes.inspect} without creating partial configuration" do
       question = r09_question('team_size', answer_type: 'number', prompt: 'How many people work in your team?')
-      rule = { kind: 'score_rule', priority: 0, score_delta: 30, enabled: true }.merge(invalid_attributes)
+      rule = rule_defaults.merge(invalid_attributes)
 
       post r09_offers_url, headers: r09_headers,
                            params: { offer: r09_configuration(questions: [question], rules: [rule]) }, as: :json
diff --git a/spec/services/ai_lead_employee/follow_up_migration_spec.rb b/spec/services/ai_lead_employee/add_follow_up_attempt_lineage_spec.rb
similarity index 93%
rename from spec/services/ai_lead_employee/follow_up_migration_spec.rb
rename to spec/services/ai_lead_employee/add_follow_up_attempt_lineage_spec.rb
index d82608d..fbe3bf0 100644
--- a/spec/services/ai_lead_employee/follow_up_migration_spec.rb
+++ b/spec/services/ai_lead_employee/add_follow_up_attempt_lineage_spec.rb
@@ -19,7 +19,7 @@ RSpec.describe AddFollowUpAttemptLineage do
   let(:database) { ActiveRecord::Base.connection }
 
   before do
-    database.execute <<~SQL
+    database.execute <<~SQL.squish
       CREATE TABLE accounts (id bigint PRIMARY KEY);
       CREATE TABLE contacts (id bigint PRIMARY KEY);
       CREATE TABLE ai_lead_employee_offers (id bigint PRIMARY KEY);
@@ -46,7 +46,7 @@ RSpec.describe AddFollowUpAttemptLineage do
     cases.each_with_index do |(state, status, source_id, admitted_at, _expected), index|
       id = index + 1
       offer = index.even? ? '77' : 'NULL'
-      database.execute <<~SQL
+      database.execute <<~SQL.squish
         INSERT INTO contacts VALUES (#{id});
         INSERT INTO lead_qualifications VALUES (#{id}, #{offer});
         INSERT INTO messages VALUES (#{id}, #{database.quote(source_id)});
@@ -57,7 +57,10 @@ RSpec.describe AddFollowUpAttemptLineage do
     end
     described_class.suppress_messages { described_class.new.up }
 
-    rows = database.select_all('SELECT f.*, a.offer_id, a.admission_state, a.admitted_at FROM lead_follow_ups f JOIN lead_follow_up_attempts a ON a.id=f.follow_up_attempt_id ORDER BY f.id').to_a
+    rows = database.select_all(
+      'SELECT f.*, a.offer_id, a.admission_state, a.admitted_at FROM lead_follow_ups f ' \
+      'JOIN lead_follow_up_attempts a ON a.id=f.follow_up_attempt_id ORDER BY f.id'
+    ).to_a
     expect(rows.length).to eq(cases.length)
     rows.each_with_index do |row, index|
       expect(row).to include('id' => index + 1, 'message_id' => index + 1, 'conversation_id' => 101 + index,
diff --git a/spec/services/ai_lead_employee/follow_up_scheduler_spec.rb b/spec/services/ai_lead_employee/follow_up_scheduler_spec.rb
index 14a5d42..2755f9b 100644
--- a/spec/services/ai_lead_employee/follow_up_scheduler_spec.rb
+++ b/spec/services/ai_lead_employee/follow_up_scheduler_spec.rb
@@ -118,11 +118,7 @@ RSpec.describe AiLeadEmployee::FollowUpScheduler do
     end.not_to have_enqueued_job(AiLeadEmployee::FollowUpDeliveryJob)
 
     expect(LeadFollowUp.pending.count).to eq(0)
-    expect(follow_up.reload.question_text).to eq(question.prompt)
-    expect(follow_up.qualification_question).to eq(question)
-    expect(follow_up.content).to eq(original_content)
-    expect(follow_up.cancellation_reason).to eq('legacy_question_changed')
-    expect(follow_up.follow_up_attempt).to be_blocked
+    verify_preserved_legacy_content(follow_up, original_content)
     AiLeadEmployee::FollowUpDeliveryService.new(follow_up: follow_up).perform
     expect(follow_up.reload.message_id).to be_nil
   end
@@ -191,4 +187,12 @@ RSpec.describe AiLeadEmployee::FollowUpScheduler do
     expect(follow_up.reload).to be_cancelled
     expect(follow_up.cancellation_reason).to eq('follow_up_state_call_booked')
   end
+
+  def verify_preserved_legacy_content(follow_up, original_content)
+    expect(follow_up.reload.question_text).to eq(question.prompt)
+    expect(follow_up.qualification_question).to eq(question)
+    expect(follow_up.content).to eq(original_content)
+    expect(follow_up.cancellation_reason).to eq('legacy_question_changed')
+    expect(follow_up.follow_up_attempt).to be_blocked
+  end
 end
diff --git a/spec/services/ai_lead_employee/offer_configuration_concurrency_spec.rb b/spec/services/ai_lead_employee/offer_configuration_writer_concurrency_spec.rb
similarity index 95%
rename from spec/services/ai_lead_employee/offer_configuration_concurrency_spec.rb
rename to spec/services/ai_lead_employee/offer_configuration_writer_concurrency_spec.rb
index 2a43596..93bffc7 100644
--- a/spec/services/ai_lead_employee/offer_configuration_concurrency_spec.rb
+++ b/spec/services/ai_lead_employee/offer_configuration_writer_concurrency_spec.rb
@@ -3,7 +3,8 @@
 require 'rails_helper'
 require 'timeout'
 
-RSpec.describe 'Offer evaluation and configuration serialization' do
+# Offer evaluation and configuration serialization.
+RSpec.describe AiLeadEmployee::OfferConfigurationWriter do
   self.use_transactional_tests = false
 
   let(:workers) { [] }
@@ -11,7 +12,7 @@ RSpec.describe 'Offer evaluation and configuration serialization' do
   let(:scenario) do
     account = create(:account)
     conversation = create(:conversation, account: account)
-    offer = AiLeadEmployee::OfferConfigurationWriter.new(offer: account.qualification_offers.new, attributes: configuration).perform
+    offer = described_class.new(offer: account.qualification_offers.new, attributes: configuration).perform
     conversation.update!(offer: offer)
     message = create(:message, account: account, conversation: conversation, inbox: conversation.inbox,
                                sender: conversation.contact, message_type: :incoming, content: 'My budget is TZS 600000.')
@@ -78,7 +79,7 @@ RSpec.describe 'Offer evaluation and configuration serialization' do
       member = create(:user, account: conversation.account, role: :agent)
       conversation.update!(assignee: member)
       headers = member.create_new_auth_token
-      replacement = AiLeadEmployee::OfferConfigurationWriter.new(
+      replacement = described_class.new(
         offer: conversation.account.qualification_offers.new, attributes: configuration.merge(name: 'Another Offer')
       ).perform
       request_worker = nil
@@ -119,7 +120,7 @@ RSpec.describe 'Offer evaluation and configuration serialization' do
     offer = AiLeadEmployee::Offer.find(scenario.fetch(:offer).id)
     attributes = offer.payload
     attributes.fetch('budget_ranges').first['minimum'] = '900000.00'
-    AiLeadEmployee::OfferConfigurationWriter.new(offer: offer, attributes: attributes).perform
+    described_class.new(offer: offer, attributes: attributes).perform
   end
 
   def select_offer_request(headers, offer_id)
diff --git a/spec/services/ai_lead_employee/purchase_budget_capacity_spec.rb b/spec/services/ai_lead_employee/qualification_evidence_extractor_purchase_budget_capacity_spec.rb
similarity index 72%
rename from spec/services/ai_lead_employee/purchase_budget_capacity_spec.rb
rename to spec/services/ai_lead_employee/qualification_evidence_extractor_purchase_budget_capacity_spec.rb
index 0703ecf..ccd5123 100644
--- a/spec/services/ai_lead_employee/purchase_budget_capacity_spec.rb
+++ b/spec/services/ai_lead_employee/qualification_evidence_extractor_purchase_budget_capacity_spec.rb
@@ -9,16 +9,17 @@ require_relative '../../../app/services/ai_lead_employee/qualification_amount_pa
 require_relative '../../../app/services/ai_lead_employee/qualification_budget_evidence_extractor'
 require_relative '../../../app/services/ai_lead_employee/qualification_evidence_extractor'
 
-RSpec.describe 'Purchase budget capacity evidence' do
+# Purchase budget capacity evidence.
+RSpec.describe AiLeadEmployee::QualificationEvidenceExtractor do
   it 'records stated purchase capacity without claiming committed funds' do
     statement = 'I need more leads now. I am the owner of the agency and can spend $2500.'
-    budget = AiLeadEmployee::QualificationEvidenceExtractor.new(statement).observations.fetch('budget')
+    budget = described_class.new(statement).observations.fetch('budget')
 
     expect(budget).to include('polarity' => 'positive', 'amount_minor' => 250_000, 'currency' => 'USD', 'basis' => 'capacity')
   end
 
   it 'keeps an explicitly allocated budget distinct from spending capacity' do
-    budget = AiLeadEmployee::QualificationEvidenceExtractor.new('I have set aside $2500 for this purchase.').observations.fetch('budget')
+    budget = described_class.new('I have set aside $2500 for this purchase.').observations.fetch('budget')
     expect(budget).to include('polarity' => 'positive', 'amount_minor' => 250_000, 'basis' => 'allocation')
   end
 
@@ -31,33 +32,33 @@ RSpec.describe 'Purchase budget capacity evidence' do
     'I can spend $2500 depending on approval.'
   ].each do |statement|
     it "does not treat #{statement.inspect} as positive purchase capacity" do
-      budget = AiLeadEmployee::QualificationEvidenceExtractor.new(statement).observations['budget']
+      budget = described_class.new(statement).observations['budget']
       expect(budget&.fetch('polarity', nil)).not_to eq('positive')
     end
   end
 
   it 'preserves unknown currency instead of claiming known-currency sufficiency' do
-    budget = AiLeadEmployee::QualificationEvidenceExtractor.new('My budget is 2500.').observations.fetch('budget')
+    budget = described_class.new('My budget is 2500.').observations.fetch('budget')
     expect(budget).to include('currency' => nil)
   end
 
   it 'retains positive purchase capacity when a separate contact clause offers a choice of contact methods' do
     statement = 'I can spend $2500, and I can be reached by email or phone.'
-    budget = AiLeadEmployee::QualificationEvidenceExtractor.new(statement).observations.fetch('budget')
+    budget = described_class.new(statement).observations.fetch('budget')
 
     expect(budget).to include('basis' => 'capacity', 'polarity' => 'positive', 'amount_minor' => 250_000)
   end
 
   it 'retains a funding condition after a separate contact clause' do
     statement = 'I can spend $2500, and I can be reached by email or phone, depending on approval.'
-    budget = AiLeadEmployee::QualificationEvidenceExtractor.new(statement).observations.fetch('budget')
+    budget = described_class.new(statement).observations.fetch('budget')
 
     expect(budget).to include('polarity' => 'unknown')
   end
 
   it 'does not drop a funding condition before the spending statement' do
     statement = 'If my loan is approved, I can spend $2500.'
-    budget = AiLeadEmployee::QualificationEvidenceExtractor.new(statement).observations.fetch('budget')
+    budget = described_class.new(statement).observations.fetch('budget')
 
     expect(budget).to include('polarity' => 'unknown')
   end
diff --git a/spec/services/ai_lead_employee/purchase_capacity_continuations_spec.rb b/spec/services/ai_lead_employee/qualification_evidence_extractor_purchase_capacity_continuations_spec.rb
similarity index 82%
rename from spec/services/ai_lead_employee/purchase_capacity_continuations_spec.rb
rename to spec/services/ai_lead_employee/qualification_evidence_extractor_purchase_capacity_continuations_spec.rb
index 2ad5771..fb03043 100644
--- a/spec/services/ai_lead_employee/purchase_capacity_continuations_spec.rb
+++ b/spec/services/ai_lead_employee/qualification_evidence_extractor_purchase_capacity_continuations_spec.rb
@@ -9,7 +9,8 @@ require_relative '../../../app/services/ai_lead_employee/qualification_amount_pa
 require_relative '../../../app/services/ai_lead_employee/qualification_budget_evidence_extractor'
 require_relative '../../../app/services/ai_lead_employee/qualification_evidence_extractor'
 
-RSpec.describe 'Purchase capacity with an adversative continuation' do
+# Purchase capacity with an adversative continuation.
+RSpec.describe AiLeadEmployee::QualificationEvidenceExtractor do
   [
     'I can spend $2500, but only if my loan is approved.',
     'I can spend $2500, but my loan must be approved.',
@@ -27,7 +28,7 @@ RSpec.describe 'Purchase capacity with an adversative continuation' do
     'Ninaweza kutumia TZS 600000, lakini siwezi kutumia pesa hizo sasa.'
   ].each do |statement|
     it "does not strip the restriction from #{statement.inspect}" do
-      budget = AiLeadEmployee::QualificationEvidenceExtractor.new(statement).observations['budget']
+      budget = described_class.new(statement).observations['budget']
 
       expect(budget&.fetch('polarity', nil)).not_to eq('positive')
     end
@@ -39,21 +40,21 @@ RSpec.describe 'Purchase capacity with an adversative continuation' do
     ['I cannot spend $2500, but I can spend $1000.', 100_000, 'USD']
   ].each do |statement, amount_minor, currency|
     it "preserves the independent budget correction in #{statement.inspect}" do
-      budget = AiLeadEmployee::QualificationEvidenceExtractor.new(statement).observations.fetch('budget')
+      budget = described_class.new(statement).observations.fetch('budget')
 
       expect(budget).to include('polarity' => 'positive', 'amount_minor' => amount_minor, 'currency' => currency)
     end
   end
 
   it 'retains an independent authority denial without discarding an unconditional budget' do
-    facts = AiLeadEmployee::QualificationEvidenceExtractor.new('I can spend $2500, but I am not the decision maker.').observations
+    facts = described_class.new('I can spend $2500, but I am not the decision maker.').observations
 
     expect(facts.fetch('budget')).to include('polarity' => 'positive', 'amount_minor' => 250_000)
     expect(facts.fetch('decision_authority')).to include('polarity' => 'negative')
   end
 
   it 'retains an unconditional Swahili purchase-capacity assertion' do
-    budget = AiLeadEmployee::QualificationEvidenceExtractor.new('Ninaweza kutumia TZS 600000 kwa huduma hii.').observations.fetch('budget')
+    budget = described_class.new('Ninaweza kutumia TZS 600000 kwa huduma hii.').observations.fetch('budget')
 
     expect(budget).to include('polarity' => 'positive', 'basis' => 'capacity', 'amount_minor' => 60_000_000, 'currency' => 'TZS')
   end
@@ -63,7 +64,7 @@ RSpec.describe 'Purchase capacity with an adversative continuation' do
     ['I can spend $2500, but I am not sure what problem I have.', 'problem']
   ].each do |statement, other_signal|
     it "does not apply independent #{other_signal} uncertainty to the budget" do
-      facts = AiLeadEmployee::QualificationEvidenceExtractor.new(statement).observations
+      facts = described_class.new(statement).observations
 
       expect(facts.fetch('budget')).to include('polarity' => 'positive', 'amount_minor' => 250_000)
       expect(facts.fetch(other_signal)).to include('polarity' => 'unknown')
diff --git a/spec/support/offer_qualification_requests.rb b/spec/support/offer_qualification_requests.rb
index 5c4005a..9c4a049 100644
--- a/spec/support/offer_qualification_requests.rb
+++ b/spec/support/offer_qualification_requests.rb
@@ -52,9 +52,17 @@ module OfferQualificationRequests
   def r09_offers_url
     "/api/v1/accounts/#{account.id}/qualification_offers"
   end
+
+  def r09_prepare_request_context
+    create(:ai_provider_connection, account: account)
+    allow(AiLeadEmployee::LaunchGate).to receive(:live_ai_enabled?).with(account).and_return(true)
+    allow(AiLeadEmployee::OutboxDispatchJob).to receive(:perform_later)
+    allow(SendReplyJob).to receive(:perform_later)
+    expect(AiLeadEmployee::AiProvider::ClientFactory).not_to receive(:for)
+  end
 end
 
-RSpec.shared_context 'Offer qualification requests' do
+RSpec.shared_context 'with Offer qualification requests' do
   include OfferQualificationRequests
 
   let(:account) { create(:account) }
@@ -66,10 +74,6 @@ RSpec.shared_context 'Offer qualification requests' do
   end
 
   before do
-    create(:ai_provider_connection, account: account)
-    allow(AiLeadEmployee::LaunchGate).to receive(:live_ai_enabled?).with(account).and_return(true)
-    allow(AiLeadEmployee::OutboxDispatchJob).to receive(:perform_later)
-    allow(SendReplyJob).to receive(:perform_later)
-    expect(AiLeadEmployee::AiProvider::ClientFactory).not_to receive(:for)
+    r09_prepare_request_context
   end
 end
