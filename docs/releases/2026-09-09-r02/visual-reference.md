# R02 visual reference

Prepared before implementation expansion, from the existing CE Rails/Vue target and the frozen 9 September audit (desktop screenshot 03, phone 24). The adjacent desktop and phone SVGs specify layout intent, not completed or live behavior.

Use CE n-slate/n-background tokens, 14px body text, 20–24px page titles, 8px spacing multiples, 40–44px controls, permanent accessible names and visible keyboard focus. Protect the Lead name; show control and assignee on a separate wrapping line. Secondary details open on demand. At widths below 1024px show either the selectable list or the selected Conversation, never an empty hidden list. All is the default queue. Filters are optional and URL-backed. Five desktop destinations; four phone tabs with Knowledge and Settings in More. Settings retains its section navigation for nested CE pages and AI & testing.

Implementation and browser evidence will be captured separately. Visual references use synthetic content and do not establish provider behavior.
